#include <iostream>
using namespace std;
#include <string>
#include <cmath>
#include "circle.h"


int main() {
    int cx, cy, r, px, py;

    cx = 0;
    cy = 0;
    r = 5;
    px = 3;
    py = 4;

    int d = sqrt( pow(px, 2) + pow(py, 2) );
    int rr = pow(r, 2);

    
}